# agentcore-with-dspy
Example of an agent programmed with dspy and deployed with agentcore  
.bedrock_agentcore.yaml not uploaded by privacy reasons, be sure to set `api_key_credential_provider_name: dspy` on it to make Bedrock access not required
